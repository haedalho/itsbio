name: Apply Kent gallery data architecture

on:
  pull_request:
    branches:
      - main
    paths:
      - scripts/patch-kent-gallery-no-heuristics.mjs
      - .github/workflows/apply-kent-gallery-data-architecture.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout working branch
        uses: actions/checkout@v4
        with:
          ref: agent/kent-preflight
          fetch-depth: 0

      - name: Remove gallery heuristics
        run: node scripts/patch-kent-gallery-no-heuristics.mjs

      - name: Commit generated source changes
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add scripts/kent-official-gallery-promote.mjs
          if git diff --cached --quiet; then
            echo "No generated changes"
            exit 0
          fi
          git commit -m "Promote exact approved Kent gallery images"
          git push origin HEAD:agent/kent-preflight
