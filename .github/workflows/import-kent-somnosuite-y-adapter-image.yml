name: Import Kent 2-Accessory Connector image

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired. Kent media must be reviewed and uploaded to Sanity; supplier hotlink/download automation is disabled."
